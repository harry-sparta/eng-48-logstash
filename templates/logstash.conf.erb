# Logstash configuration
## Order of communitcations:  Filebeat > Logstash > Elasticsearch > Kibana
## Inputs - outputs from filebeat
## Outputs - inputs for elasticsearch

# Inputs
input {
  beats {
    port => 5044
  }
}

# Filter
# Not yet set.

# Outputs
output {
  elasticsearch {
    hosts => ["localhost:9200"]
    sniffing => true
    manage_template => false
    index => "%{[@metadata][beat]}-%{+YYYY.MM.dd}"
    document_type => "%{[@metadata][type]}"
  }
}
